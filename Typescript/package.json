{
  "name": "ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --exec tsc src/app.ts --outDir dist --target es2015 || exit 1",
    "devtest": "nodemon --exec tsc src/test.ts --outDir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^3.0.3",
    "typescript": "^5.3.3"
  }
}
